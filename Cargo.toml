[package]
name = "csv_parser"
version = "0.1.0"
edition = "2024"

[dependencies]
memchr = "2.8.3"

[dev-dependencies]
criterion = "0.5"
pprof = { version = "0.13", features = ["flamegraph", "criterion"] }

[[bench]]
name = "parse"
harness = false
